import { ModulePage } from "../../src/components/module-page";

export default function AssignmentsPage() {
  return (
    <ModulePage
      title="Assignments"
      summary="Generalized customer-to-asset allocations with room for rental-style workflows."
      statusNote="The scaffold fixes the core noun now so later work can stay product-friendly without being locked to one business model."
    />
  );
}
