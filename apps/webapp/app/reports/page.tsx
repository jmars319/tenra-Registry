import { ModulePage } from "../../src/components/module-page";

export default function ReportsPage() {
  return (
    <ModulePage
      title="Reports"
      summary="Future visibility across operations, balances, and asset usage trends."
      statusNote="Reporting remains intentionally light until the first production workflows establish the right data shape."
    />
  );
}
